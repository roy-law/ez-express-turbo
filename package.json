{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "start": "turbo start",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev:doc": "npm run dev -w apps/docs",
    "build:client": "npm run build -w apps/ez-express-client",
    "build:admin": "npm run build -w apps/ez-express-admin",
    "build:server": "npm run build -w apps/ez-express-server"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "@repo/eslint-config": "*",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "npm@8.19.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
